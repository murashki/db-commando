import { __STRING_REPLACE_MAP_RENDER_SYMBOL__ } from '../constants.ts';

export type DbTableStringReplaceMapRender = {
  type: typeof __STRING_REPLACE_MAP_RENDER_SYMBOL__;
  replaceMap: Record<string, null | boolean | number | string>;
};
