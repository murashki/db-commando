import { __STRING_REPLACE_RENDER_SYMBOL__ } from '../constants.ts';

export type DbTableStringReplaceRender = {
  type: typeof __STRING_REPLACE_RENDER_SYMBOL__;
  pattern: string;
  replacement: string;
};
